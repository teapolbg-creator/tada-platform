import { ComingSoon } from '../src/components/ComingSoon';

/**
 * Placeholder. The real call simulation screens (matching mockups #4, #5, #6)
 * land in the next conversation. They render the in-app call UI while the
 * real outbound Hubtel Voice call rings the user's phone in parallel.
 */
export default function Call() {
  return <ComingSoon title="Emergency Call" />;
}
