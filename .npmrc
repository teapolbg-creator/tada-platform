# Hoist all transitive deps so React Native / Metro / Expo can resolve
# packages like expo-splash-screen and @babel/runtime that get pulled in
# implicitly by expo-router. Required for pnpm + Expo monorepos.
shamefully-hoist=true
